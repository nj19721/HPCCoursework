This directory was included to demonstrate the vectorised code that did not make it into the final parallel implementation.

To compile and run, make sure

module load icc/2017.1.132-GCC-5.4.0-2.26

has been executed. Then simply type 'make'.